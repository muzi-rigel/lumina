# AGENTS.md

本文件定义本仓库中所有 AI 开发助手必须遵守的长期开发规范。除非用户在当前任务中明确给出更高优先级的指令，否则后续分析、设计、编码、测试和交付均须遵守本文件。

## 一、项目背景

项目名称为 **Lumina**。

Lumina 是长期运行在 Ubuntu 22.04 LTS 云服务器上的 A 股智能监控与研究系统。

主要功能：

1. 获取 A 股指数、ETF、个股实时行情。
2. 发现市场异动，并根据规则进行判断。
3. 通过企业微信机器人推送提醒。
4. 保存历史行情数据。
5. 后续支持市场分析、策略验证、AI 辅助研究和 Web 页面。

### 命名约定

- 产品名称和面向用户的正式名称使用 `Lumina`。
- 代码包、模块命名空间、目录、配置文件、数据库文件、日志文件、进程标识和 systemd 服务等技术标识统一使用小写 `lumina`。
- 示例：Python 包使用 `lumina`，systemd 单元使用 `lumina.service`，主配置文件使用 `settings.yaml`，监控标的配置使用 `stocks.yaml`。
- 不得为本项目引入其他项目简称、大小写变体或无关的服务名称。

## 二、技术约定

- Python：3.11 及以上版本。
- 操作系统：Ubuntu 22.04 LTS。
- 数据库：初期使用 SQLite，后续可能迁移至 MySQL 或 PostgreSQL。
- 服务运行方式：systemd。
- 配置方式：YAML 文件。
- 日志方式：Python 标准日志系统。
- 代码管理：Git。

实现时应注意 Ubuntu 与 Python 3.11+ 的兼容性，避免依赖仅适用于本地开发系统的行为。

## 三、代码规范

1. 单个源代码文件不得超过 300 行；接近上限时应按职责合理拆分。
2. 功能必须模块化。
3. 一个模块只负责一个明确职责。
4. 不得为了快速实现而直接复制重复代码。
5. 可复用的公共方法必须抽取到合适的公共模块。
6. 新增或修改的 Python 代码必须使用类型提示。
7. 关键函数和复杂逻辑必须添加清晰、必要的中文注释。
8. 所有可预见异常必须妥善处理，禁止使用裸 `except`。
9. 不得为了减少代码量而牺牲可读性、可测试性和可维护性。

## 四、修改已有代码的流程

修改已有代码前，必须依次完成：

1. 分析当前代码结构和相关调用关系。
2. 向用户说明修改方案及设计原因。
3. 明确影响范围，包括相关模块、配置、数据、部署和兼容性。
4. 再进行代码修改。

不得在未经用户确认的情况下进行大规模重构。

当任务属于“大改”时，必须先输出标题为 **“实施计划（Plan）”** 的计划，等待用户确认后再执行。大改包括但不限于：

- 跨多个核心模块的结构调整。
- 数据库结构或数据迁移方案变更。
- 行情采集、规则判断、推送或调度主链路重写。
- 引入新的服务组件或替换关键技术方案。
- 会显著改变部署方式、运行行为或外部接口的修改。

小范围、低风险修改可以在完成结构分析、方案说明和影响范围说明后执行；如果无法确定是否属于大改，应先征求用户确认。

## 五、稳定性要求

该程序需要长期稳定运行，设计和实现必须满足：

- 网络异常后能够自动恢复。
- API 调用失败应采用合理的重试、退避和超时策略。
- 日志必须记录关键运行信息与异常上下文。
- 单个股票、指数或 ETF 的数据异常不得导致整个服务退出。
- 对外部数据应进行必要的校验和容错。
- 应合理隔离单次任务失败，避免影响后续调度周期。
- 异常处理不得静默吞掉错误；无法恢复的异常应明确记录。

## 六、设计原则

设计优先级：

**稳定性 > 性能 > 代码简洁**

项目为个人长期维护项目，不追求不必要的复杂架构：

- 优先选择简单、可靠、可维护的方案。
- 除非用户明确提出，否则不要引入 Redis、消息队列、微服务等复杂组件。
- 数据访问层应避免与 SQLite 细节过度耦合，为未来迁移 MySQL 或 PostgreSQL 保留合理边界，但不得提前过度设计。
- 新增依赖前应说明必要性、维护成本和替代方案。
- 方案应具备工程可用性，不应只提供入门教程式实现。

## 七、需求沟通与交付

- 用户具备较强 Python 开发经验，理解线程、网络和数据库，可直接讨论工程细节与权衡。
- 如果需求存在不明确之处，应先提出问题，不得自行作出可能改变实现方向的关键假设。
- 每次修改都应解释设计原因。
- 完成修改后，应说明实际改动、影响范围、验证方式、验证结果和仍存在的风险。
- 测试和验证应与修改风险相匹配；涉及长期运行、重试、数据持久化或告警链路时，应重点验证失败场景。
- 不得声称未实际执行的测试已经通过。

## Development workflow

For each new phase:

1. First perform read-only analysis.
2. Produce an implementation plan.
3. Wait for confirmation before modifying code.
4. Keep changes scoped to the phase goal.
5. Run tests and static checks after implementation.
6. Do not introduce new infrastructure unless explicitly justified.
7. Prefer simple reliable solutions over enterprise complexity.
