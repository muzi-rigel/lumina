# Lumina 本地默认配置；生产环境部署到 /etc/lumina/lumina.yaml。
scheduler:
  interval_seconds: 30

logging:
  level: INFO
  # systemd 环境建议使用 null，由 journald 统一收集日志。
  file: null
  max_bytes: 10485760
  backup_count: 10

storage:
  database: data/lumina.db
  busy_timeout_seconds: 10
